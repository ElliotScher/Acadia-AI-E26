# Test Images

Place test images (e.g., `.jpg`, `.jpeg`, `.png`, `.bmp`) in this directory.

### Used For:
- Testing object detection (e.g., YOLO bounding box verification).
- Testing entity profiling and re-identification (Re-ID matching) across images.
- Verification of image utilities like center-cropping and HSV histogram calculations.
